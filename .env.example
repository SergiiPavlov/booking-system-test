# Database
# Local (Docker): postgres://postgres:postgres@localhost:5432/booking
# Neon: use two URLs
# - DATABASE_URL: pooled (host contains "-pooler") for app runtime
# - DIRECT_URL: direct (host without "-pooler") for Prisma migrations
DATABASE_URL="postgres://postgres:postgres@localhost:5432/booking"
DIRECT_URL="postgres://postgres:postgres@localhost:5432/booking"

# Auth (JWT)
JWT_SECRET="change-me-in-prod"

# App URL (optional)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
